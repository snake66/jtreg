name: Build and Test JTReg

on:
  push:
    branches-ignore:
      - master
      - pr/*

jobs:

  linux-x64:
    runs-on: ubuntu-latest

    steps:
    - name: 'Check out repository'
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: 'Set up Java Development Kit'
      uses: oracle-actions/setup-java@v1
      with:
        website: oracle.com
        release: 21

    - name: 'Build JTReg'
      shell: bash
      run: |
        java --version
        bash make/build.sh

    - name: 'Run initial tests (goal: quick-test)'
      shell: bash
      env:
        MAKE_ARGS: quick-test
        HEADLESS: 1
      run: |
        bash make/build.sh --skip-download

    - name: 'Run all tests (goal: test)'
      shell: bash
      env:
        MAKE_ARGS: test
        HEADLESS: 1
      run: |
        bash make/build.sh --skip-download

  freebsd-x64:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Initialize FreeBSD VM
        id: vm
        uses: vmactions/freebsd-vm@v1
        with:
          sync: nfs
          prepare: |
            pkg install -y bash curl git gmake gnugrep openjdk21 zip
            mount -t fdescfs fdesc /dev/fd
            mount -t procfs proc /proc

          run: |
            pwd
            ls -lah
            whoami
            env
            freebsd-version
            sysctl hw.model
            sysctl hw.ncpu
            sysctl hw.physmem
            sysctl hw.usermem
            pw user add -n action -m

        - name: Build jtreg
          id: build
          shell: freebsd {0}
          run: |
            su action -c 'git config --global --add safe.directory /home/runner/work/jtreg/jtreg && bash make/build.sh --jdk /usr/local/openjdk21'

        - name: Run tests
          id: test
          shell: freebsd {0}
          run: |
            su action -c 'bash build/make.sh test'
